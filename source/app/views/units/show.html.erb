<div class="">

    <div class="tab-pane" id="<%= @challenges.first.unit.phase.name%>">
      <ul class="nav nav-tabs nav-stacked content">
        <% @challenges.each do |challenge| %>
          <li><%= link_to challenge.name, challenge_path(challenge), {:"data-toggle" => "##{challenge.name}",:remote => true, :class => "challenge_link"} %></li>
        <% end %>
      </ul>
    </div>

</div>
